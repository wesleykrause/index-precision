// The real product is a fully static site served from /public (index.html + css + js).
// This React entry renders nothing so it never overlays the static page during preview.
function App() {
  return null;
}

export default App;
